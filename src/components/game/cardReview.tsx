import PixiText from "../core/PixiText";

const CardReview: React.FC = () => {
  return (
    <PixiText
        text="Card Preview"
        x={800}
        y={680}
        textSize={12}
        name="cardPreview"
        cursor={true}
      />
  );
};
export default CardReview;
